# A股数据查询工具 - 使用说明

**版本**: v3.2  
**最后更新**: 2026-06-01

---

## 📦 快速开始

### 1. 获取程序

你只需要一个文件：
- `Akshare.exe` (约 58 MB)

或者使用 Python 运行：
- `Akshare.py` + `akshare_app/` 目录

### 2. 运行程序

**方式一：Windows 电脑（EXE）**：
1. 双击 `Akshare.exe` 文件
2. 等待几秒钟，程序会自动启动（首次启动会预热数据缓存，约 5-15 秒）
3. 看到主菜单就可以开始使用了

**方式二：Python 脚本**：
```bash
# 安装依赖
pip install -r requirements.txt

# 启动程序（两种方式均可）
python Akshare.py          # 薄包装入口
python -m akshare_app      # 包模块入口
```

> 💡 **提示**: 第一次运行可能需要一些时间加载缓存，请耐心等待。后续启动会更快。

---

## 🎯 主要功能

### 📊 功能 1: 批量数据导出
导出多只股票的多种数据到 Excel 文件

**支持 25 种数据类型**：
- 实时行情 (L1)
- 五档盘口 (L2)
- 历史K线
- 资金流向
- 财务报表
- 分红送配
- 行业板块 / 概念板块
- 龙虎榜 / 龙虎榜详情
- 大宗交易
- 融资融券
- 北向资金持股
- 新股申购
- 股票回购
- 宏观数据（GDP/CPI/PPI/货币供应量/汇率/国债收益率/贸易余额/外商投资等）

**操作步骤**：
1. 在主菜单输入 `1`，按回车
2. 选择要导出的数据类型（输入 `0` 导出全部，或输入数字如 `1 3 5` 选择部分）
3. 确认开始获取数据
4. 等待数据下载完成（并发获取，速度提升 3-4 倍）
5. Excel 文件会自动保存在程序同目录下

---

### 🔍 功能 2: 单股分析查询
深入分析单只股票的详细数据

**包含的内容**：
- 实时行情和最新价格
- 历史K线数据和技术指标（MA、MACD、RSI、KDJ、布林带、动量等）
- L2 五档盘口
- 资金流向
- 财务报表
- 分红送配记录
- 行业信息
- 可选保存为 Excel 报告
- 可选 AI 智能分析报告

**操作步骤**：
1. 在主菜单输入 `2`，按回车
2. 输入 6 位股票代码（如 `688275`）
3. 查看详细分析结果
4. 可选择保存到 Excel（输入 `Y`）
5. 可选择调用 AI 分析（输入 `Y`，需配置 API Key）

---

### 🎯 功能 3: 股票智能筛选
根据策略自动筛选符合条件的股票

**支持三种筛选策略**：
1. **短线强势股** — 适合短期交易（均线金叉、放量突破、RSI强势、DDX大单动向、主力净流入）
2. **主力建仓股** — 适合中期布局（价格低位、成交量趋势、MACD金叉、DDX/DDY持续为正、主力净流入）
3. **价值投资股** — 适合长期持有（PE/PB估值合理、MACD零轴上方、股价相对低位）

**策略评分说明**：
- 每种策略满分 100 分，包含 5-6 项评分指标
- 不设通过阈值，所有评分结果均保留供用户自行判断
- 选择"全部策略"时，综合得分为三种策略得分的平均值
- 批量筛选默认推荐 Top 5 股票，可通过菜单调整推荐数量（1-20）

**操作步骤**：
1. 在主菜单输入 `3`，按回车
2. 选择功能：[1] 单股筛选 / [2] 批量智能筛选 / [3] 切换推荐数量
3. 输入股票代码或选择筛选策略
4. 等待筛选完成
5. 查看推荐股票列表及评分
6. 可选择导出为 CSV 或 Excel
7. 可选择 AI 深度分析

---

### 🚪 退出程序

在主菜单输入 `q`，按回车即可退出

---

## 💡 使用技巧

### 股票代码输入格式
- **上海股票**: `600000`、`600519` 等（6 位数字）
- **深圳股票**: `000001`、`000002` 等（6 位数字）
- **创业板**: `300001`、`300750` 等（6 位数字）
- **科创板**: `688001`、`688275` 等（6 位数字）

> ⚠️ **注意**: 必须输入完整的 6 位数字代码。输入 5 位或其他格式会被系统自动拦截。

### 数据获取时间
- 单只股票分析: 约 5-15 秒（取决于网络和缓存命中情况）
- 单只策略评分: 约 0.3-1 秒（缓存命中时），首次约 7 秒（含数据加载）
- 批量数据导出: 取决于数据类型数量，并发获取速度提升 3-4 倍
- 批量智能筛选: 约 60-90 秒（200 支候选股采样，5 线程并发）
- 程序内置 4 级数据源自动切换，网络异常时自动重试
- 缓存失败后 30 秒冷却，避免重复请求导致 API 限流

### 缓存预热
程序启动后会自动在后台预加载全市场行情数据，预热完成后查询速度会大幅提升。预热过程中不影响正常使用。预热数据包含全市场股票名称映射，单股查询时名称获取为 O(1) 即时响应。缓存大小上限 100MB，超出后自动清理最早的缓存条目。

### 网络要求
程序需要联网获取股票数据，请确保：
- 电脑已连接互联网
- 网络连接稳定
- 如遇限流，等待 1-2 分钟重试即可

---

## 🏗️ 项目结构

```
akshare_app/                    # 核心包
├── __init__.py                 # 包初始化 & 统一导出
├── config.py                   # 配置常量、环境变量、全局资源
├── logging_utils.py            # 日志和 Rich 控制台工具
├── utils.py                    # 通用工具函数
├── cache.py                    # 缓存管理（预热/刷新/大小限制）
├── indicators.py               # 技术指标计算（RSI/MACD/KDJ/布林带）
├── data_fetcher.py             # 数据获取层（L1/L2/历史K线/财务）
├── analysis.py                 # 单股分析逻辑
├── market_data.py              # 市场数据批量获取（并发优化）
├── ai_analysis.py              # AI 分析（DashScope/通义千问）
├── export.py                   # Excel/CSV 导出
├── display.py                  # Rich 表格显示
├── main.py                     # 主菜单 & 程序入口
└── screeners/                  # 筛选器子包
    ├── __init__.py             # 筛选器统一导出
    ├── l1_screener.py          # L1 行情数据筛选
    ├── l2_screener.py          # L2 盘口数据筛选
    ├── strategies.py           # 三种策略评分引擎
    └── stock_screener.py       # 智能筛选调度器
```

---

## ⚠️ 常见问题

### Q: 杀毒软件提示有风险？
**A**: 这是正常的，因为这是一个独立打包的程序。请选择"允许运行"或"更多信息" → "仍要运行"。本程序不包含任何恶意代码，不会收集或上传个人信息。

### Q: 程序启动很慢？
**A**: 首次运行需要解压依赖文件并预热行情缓存，约 5-15 秒。后续启动会更快。预热期间可以正常使用程序。

### Q: 获取数据失败 / 显示 ConnectionError？
**A**: 请依次排查：
1. 网络连接是否正常
2. 股票代码是否为 6 位纯数字
3. 稍等 1-2 分钟重试（API 可能限流）
4. 程序内置了腾讯 K 线、新浪财经、Tushare、yfinance 四种数据源，会自动切换
5. 缓存失败后有 30 秒冷却期，期间使用上次缓存数据，冷却结束后自动恢复

### Q: 批量筛选为什么只扫描 200 支股票？
**A**: 全市场 A 股超过 5000 支，逐一筛选耗时过长。程序自动从候选池中随机采样 200 支进行评分，兼顾覆盖面和效率。采样结果具有随机性，不同次运行可能推荐不同股票。

### Q: 为什么输入 5 位代码会提示无效？
**A**: 系统要求输入完整的 6 位股票代码。例如 `688275` 不能简写为 `68827`。输入无效代码会被自动拦截，避免无效的网络请求。

### Q: Excel 文件在哪里？
**A**: Excel 文件会保存在和 `Akshare.exe` 同一个文件夹下，文件名包含时间和股票代码，如 `688275_分析报告.xlsx`。

### Q: 可以在 Mac 电脑上使用吗？
**A**: 当前发布的 `Akshare.exe` 仅支持 Windows 7/10/11。如需在 Mac 或 Linux 上使用，请安装 Python 环境后直接运行 `python Akshare.py`。

### Q: 如何开启 AI 分析功能？
**A**: 访问 [DashScope 官网](https://dashscope.aliyun.com) 注册获取免费 API Key，然后设置环境变量 `DASHSCOPE_API_KEY`。

### Q: 如何修改缓存大小或有效期？
**A**: 编辑 `akshare_app/config.py` 中的配置常量：
- `_CACHE_TTL`：缓存有效期（秒），默认 300（5 分钟）
- `_CACHE_MAX_SIZE_MB`：最大缓存大小（MB），默认 100
- `_CACHE_FAIL_COOLDOWN`：缓存失败冷却时间（秒），默认 30

---

## 📋 系统要求

- **操作系统**: Windows 7 或更高版本
- **内存**: 建议 2GB 以上
- **硬盘空间**: 至少 100MB 可用空间
- **网络**: 需要稳定的互联网连接
- **Python**: 如使用 EXE 版本则无需安装；如使用脚本版本需要 Python 3.11+

---

## 🔒 安全说明

- 本程序不包含任何恶意代码
- 程序只会访问公开的股票数据接口
- 不会收集或上传你的任何个人信息
- 不会修改系统设置
- 所有数据仅在本地处理
- API Key 通过环境变量读取，不硬编码在代码中

---

## 📞 技术支持

如果遇到问题：
1. 检查网络连接是否正常
2. 确认股票代码为 6 位纯数字
3. 查看程序显示的错误提示信息
4. 等待 1-2 分钟后重试（API 限流时）
5. 重新运行程序试试

项目地址：[https://github.com/delamey/Akshare](https://github.com/delamey/Akshare)

---

## 📝 更新日志

### v3.2 (2026-06-01)
- 🏗️ **模块化重构**：6300+ 行单文件拆分为 18 个模块，职责清晰
  - `config.py` — 集中配置常量和全局资源
  - `cache.py` — 缓存管理（预热/刷新/大小限制）
  - `indicators.py` — 技术指标计算（纯函数，无副作用）
  - `data_fetcher.py` — 数据获取层，多源 Fallback
  - `market_data.py` — 批量市场数据并发获取
  - `screeners/` — 筛选器子包（L1/L2/策略/智能筛选）
- 🚀 **并发优化**：ThreadPoolExecutor 并行获取 13 种独立 API 数据，批量导出速度提升 3-4 倍
- 🧠 **内存优化**：缓存大小上限 100MB，LRU 式自动清理防止内存泄漏
- 🔒 **安全修复**：API Key 改为环境变量读取，不再硬编码
- 🔧 **错误处理标准化**：`_safe_console_print` + 上下文管理器统一错误处理
- 📐 **技术指标中心化**：RSI/MACD/KDJ/布林带计算集中在 `indicators.py`，消除副作用
- 🐛 修复函数名拼写错误 `expo10rt_strategy_scores_to_csv`
- 🐛 修复缓存大小检查函数未调用的问题
- 🐛 修复 `TechnicalIndicators` 隐式修改输入 DataFrame 的副作用

### v3.1 (2026-05-31)
- 🚀 策略 all 模式一次性加载数据，避免重复加载，性能提升 74.8%
- 🚀 新增名称映射字典缓存，名称查询从 O(n) 降至 O(1)
- 🚀 候选股数量上限 200 支 + 随机采样，防止全市场遍历
- 🚀 早停阈值按策略自适应（策略1→60 / 策略2→55 / 策略3→50）
- 🚀 缓存失败 30 秒冷却机制，防止重试风暴
- 🔧 新增 _make_detail 评分明细工厂函数，代码量减少 40%
- 🔧 L2 指标值提取为局部变量，减少重复字典查找
- 🔧 TechnicalIndicators rolling 计算添加 min_periods=1，消除 NaN 传播
- 🔧 L1Screener 缓存 _latest_row/_prev_row，避免重复 iloc 查询

### v3.0 (2026-05-31)
- ✨ 新增股票代码 6 位数字校验，自动拦截无效输入
- 🚀 优化缓存预热机制，仅输出一行摘要日志
- 🔧 预热线程异常时自动检测并重启恢复
- 🚀 4 级 API Fallback 架构（腾讯K线→新浪→Tushare→yfinance）
- 🔧 修复批量筛选空列表和无效代码的问题
- 📦 完善 EXE 打包配置，补全所有依赖项

### v2.0 (2026-05)
- ✨ 新增三种股票智能筛选策略
- ✨ 新增缓存预热机制
- 🔧 修复 RemoteDisconnected 错误
- 📊 新增详细耗时日志输出

### v1.0 (2026-04)
- 🎉 初始版本，支持批量数据导出和单股分析

---

**祝您使用愉快！** 🎉
